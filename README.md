# Brief

Lokale Meeting Intelligence (Tauri 2). Kurzsetup: [`brief/README.md`](brief/README.md).

**Entwicklerdokumentation (MkDocs):** Im Repository-Root `python3 -m pip install -r requirements-docs.txt` ausführen, danach `python3 -m mkdocs serve` (Vorschau) oder `python3 -m mkdocs build --strict` (statische Site unter `site/`). Die Markdown-Quellen liegen in `brief/docs/`.